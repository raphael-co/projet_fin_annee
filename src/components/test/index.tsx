import React from 'react';
import { Alert } from 'react-bootstrap';

const TestC = () => {
  return <Alert variant={'info'}>This is a {'info'} alert—check it out!</Alert>;
};

export default TestC;
